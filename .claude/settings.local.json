{
  "permissions": {
    "allow": [
      "mcp__claude_ai_Notion__notion-fetch"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys, json; d=json.load(sys.stdin); fp=d.get('file_path',''); exit(0 if '/tools/' in fp or fp.startswith('tools/') else 1)\" && echo 'REMINDER: tools/ edited — commit this on the local branch: git checkout local && git add -f tools/ && git commit && git checkout main'"
          }
        ]
      }
    ]
  }
}
